const { userSeed } = require('./user.seeder');
const { adminSeed } = require('./admin.seeder');
const { dosenSeed } = require('./dosen.seeder');
const { mahasiswaSeed } = require('./mahasiswa.seeder');

module.exports = {
    userSeed,
    adminSeed,
    dosenSeed,
    mahasiswaSeed
};